"""Backend package for med-matcher-main.

Adding this file makes the `backend` directory a proper Python package so
relative imports (e.g. `from .ocr_utils import ...`) work when running the
module (python -m backend.main) or when using ASGI servers like uvicorn.
"""
